import { ReceiptTypePipe } from './receipt-type.pipe';

describe('ReceiptTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReceiptTypePipe();
    expect(pipe).toBeTruthy();
  });
});
